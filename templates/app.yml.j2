---
# AMQP URL for message queue (from Ansible Vault)
message_queue_url: "{{ vault_pulsar_mq }}"
conda_auto_init: false
conda_auto_install: false

managers:
  _default_:
    type: queued_python
    num_concurrent_jobs: 1